-- destination_events is currently empty and not used by the Next.js app or scoring pipeline.
-- Event demand summaries are stored in destination_event_demand.
drop table if exists public.destination_events;
